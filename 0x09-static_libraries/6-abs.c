#include "main.h"
/*
* main.h for input/output
*/

/**
* _abs - prints absolute value
* @a: value to be checed for printing
* description: function definition set for procedure
* Return: retun 1 on true and 0 if false
*/

int _abs(int a)
{
	if (a > 0)
	a = (a);
	else
	a = -(a);
	return (a);
}
